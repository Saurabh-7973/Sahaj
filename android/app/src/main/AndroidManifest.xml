<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Firebase + future cloud sync. Auto-added in debug; required for release. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Biometric app lock (local_auth). -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC"/>
    <!-- Daily reminder notifications (flutter_local_notifications).
         RECEIVE_BOOT_COMPLETED re-arms the alarm after a reboot. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <!-- Exact alarms so the daily reminder fires AT the picked minute instead
         of being deferred for hours by Doze (inexact was unreliable on real
         OEM devices — Samsung/Xiaomi). SCHEDULE_EXACT_ALARM is auto-granted on
         Android <=13; on 14+ requestExactAlarmsPermission() routes to Settings,
         falling back to inexact if denied. USE_EXACT_ALARM is stripped — Play
         restricts it to alarm-clock/calendar apps, which a wellness app is not,
         so we must not let the plugin merge it in or the listing risks rejection. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"
        tools:node="remove"/>
    <!-- Ask OEM battery managers (MIUI/One UI/ColorOS) to exempt us so Doze
         doesn't kill the scheduled fire. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>
    <application
        android:label="@string/app_name"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round">
        <!-- The launcher entry lives on activity-aliases, not MainActivity, so
             Book Mode can swap identity at runtime: enable one alias, disable
             the other (M6 mechanics). MainActivity carries no LAUNCHER filter
             to avoid a duplicate home-screen entry. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
        </activity>

        <!-- Real identity (default enabled): "Sahaj" + the lotus bud icon. -->
        <activity-alias
            android:name=".SahajLauncher"
            android:targetActivity=".MainActivity"
            android:exported="true"
            android:enabled="true"
            android:label="@string/app_name"
            android:icon="@mipmap/ic_launcher"
            android:roundIcon="@mipmap/ic_launcher_round">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity-alias>

        <!-- Disguise identity (default disabled): "My Notes" + grey-blue
             pencil icon. Book Mode flips the enabled state of the two aliases
             at runtime; until that native swap is wired this stays off so the
             home screen shows exactly one "Sahaj" entry. -->
        <activity-alias
            android:name=".NotesLauncher"
            android:targetActivity=".MainActivity"
            android:exported="true"
            android:enabled="false"
            android:label="@string/notes_label"
            android:icon="@mipmap/ic_launcher_disguise"
            android:roundIcon="@mipmap/ic_launcher_disguise_round">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity-alias>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- flutter_local_notifications: deliver scheduled reminders and
             re-arm them after a device reboot / app update. Declared here so
             AlarmManager can wake the receiver class after the app process is
             killed; without them the alarm fires but resolves to no component
             and nothing posts. -->
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ActionBroadcastReceiver" />
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Crisis-line dialing from the self-harm crisis screen (url_launcher tel:). -->
        <intent>
            <action android:name="android.intent.action.DIAL"/>
            <data android:scheme="tel"/>
        </intent>
    </queries>
</manifest>

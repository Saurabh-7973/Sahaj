/// Route paths for Sahaj. Android-only, no deep-link surface — kept flat.
class Routes {
  Routes._();

  static const onboarding = '/onboarding';
  static const today = '/today';
  static const library = '/library';
  static const me = '/me';

  /// Dev-only: visual review of the design system (Phase 1 showcase).
  static const showcase = '/showcase';
}
